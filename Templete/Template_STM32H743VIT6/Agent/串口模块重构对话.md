+ ==时间：2026年7月7日19:50:00==
+ ==内容：串口代码重构==

​	你的任务就是帮助我进行Mysystem的芯片移植代码重构，以及Mysystem核心完成之后的各个仓库的库文件重写，规范化代码，使得后续STM32移植到ESP32或者TiMSPM0等芯片只需要修改Mysystem的库即可。

​	那么你现在需要的就是首先扫描整个模板文件，得知整个模板文件的总体架构，然后协助我写本地目录的CLAUDE.md，进行长期记忆

+++

首先进行Serial的讨论，我与外界进行串口传输，到底应该怎么确定传输协议，我的需求如下

1. 首先扫描整个Serial_base和Serial模块
   1. 串口本身是否有跑飞的问题？

+++

期望的通信场景：

1. 上位机调试，发送ABC命令进行调参：

```c
if (Serial_GetNewPackageFlag_ABC(&Serial1))
    {
        // 得到数据
        Serial_SetFloatData(&Serial1, "Kp", "Kp=%f", &Kp) ;
        Serial_SetFloatData(&Serial1, "Ki", "Ki=%f", &Ki) ;
        Serial_SetFloatData(&Serial1, "Kd", "Kd=%f", &Kd) ;
    }
```

2. 设备间通信，同样类似上位机调试，但是是采用HEX数据，进行数据获取和更新，

```c
OLED_Printf(0, 20, OLED_6X8, "%d,%d,%d,%d" , 
			Serial1.Hex_Data.Serial_New_Package[0],Serial1.Hex_Data.Serial_New_Package[1],
			Serial1.Hex_Data.Serial_New_Package[2],Serial1.Hex_Data.Serial_New_Package[3]) ;
```

3. 主动发送信息，也就是Serial_Printf

4. 其实当前库就是我所期望的所有场景，我需求的是进行协议帧重构，解决下面问题：

   1. 串口在进行传输的时候传输正常，也就是头尾帧和长度正确，但是内容错误，无法被检查出来，比如本来传输1，但是传输255，导致数据传输错误

   2. 单个数据错误导致整个数据帧丢弃，效率较低

数据量级：

​	每次传输不到20个int类型数据

单向广播即可，不需要确认对方有没有收到信息



现在你开始计划怎么构建可靠的协议帧，在单向收发实现尽量可靠的数据传输

+++

​	ABC可以采用现有逻辑，一切不变，HEX可以进行修改，采用方案B，实现每个数据的逐字校验，尽量多的得到有效信息，那么你现在需要首先给我HEX协议方案，包括帧头，帧尾，内部校验规则，以及其他你觉得需要的 ，我们讨论一下再看怎么更新当前配置

+++

1. 单字校验失败使用老数据，否则可能后续写逻辑忘记0xFFFF，导致未知问题
2. 当前计划中Len的大小为0xFF，也就是256个高低位，也就是128个int数据的上限，我觉得没问题，最好是最大接收上限使用#define宏定义，后续可能多于20个字也能改#define即可
3. 帧头帧尾与数据内部冲突，有什么解决方法吗，给我方案

​	

+++

1. 单字节失败，但是整体数据还是存储在数组中，只需要不更新数组的这一位就行了不对吗

2. 支持方案1，你再给我讲讲，并且我重申我的担心：输入数据的高低位刚好是帧头两位或者帧尾两位，怎么办，怎么解决

+++

“DATA_CK 校验失败时，整帧被丢弃”，没必要，不一定所有数据都错误了，所以我觉得只需要保留单个数据的校验码，也就是下面的整体结构：

```c
帧头1 帧头2 Len 数据高位 数据低位 校验位 。。。 。。。 。。。 帧尾1 帧尾2
```

同时串口是接收一整串数据而不是一个一个接收，应该不存在再次需要寻找帧头的需求吧，每一串的第1,2数据不就是帧头吗

+++

数据出现干扰导致一帧的首个Byte不是帧头，可以先不考虑这个问题，防止问题复杂化，所以可以出现这种情况直接丢弃即可。现在开始进行计划，包括协议计划和代码清晰化重构计划，首先只保留Serial1，简单化代码，要求

1. 计划清晰
2. Serial可移植强，也就是修改结构体和部分函数即可
3. Serial2 3 加入简单
4. 暂时使用当前结构：空闲中断实现当前功能

+++

1. 

“ LEN    │ 1B   │ 字个数（最大 Serial_RX_MAX_WORDS）  ”

首先由于数据帧是高低位，也就是最终合成int16_t数据，那么LEN最好还是给出int16_t的数据个数或者是高低位个数，也就是int16_t的数据个数的两倍

2. 结构体再清晰一点，HEX和ABC的可以各自一个结构体，结构体内容你定，然后给总结构体一个Error，也就是错误码，包含HEX和ABC的错误情况，给出函数Serial_Error_Check(),给出错误原因

+++

由于Serial复杂,size可能有不同的含义，比如高低位的总数、数据帧的总数、数据的个数(高低位/2)，所以你需要重点注释

+++

1. 现在我要自发自收检测正确性，要求
   1. OLED显示发送的数据和接收的数据
   2. 发送4个即可
   3. 按键1单击就发送数据第1个加1，第2个加100，第3个加1000，第4个加0x55
   4. 按键2单击就发送数据第1个-1，第2个-100，第3个-0xFF，第4个-0x55
   5. 在Mode_2写一个函数实现上述所有功能，可以定义全局变量

+++

目前来说，你还需要进行以下修改 

1. HEX函数写在一堆
2. ABC函数写在一堆，这样后续可读性更好
3. void HAL_UARTEx_RxEventCallback(UART_HandleTypeDef *huart, uint16_t Size)里面的代码太多了，后续移植以及加入新的Serial比较麻烦，需要你封装一下
4. 考虑新增Serial2,那么怎么调整当前代码能够更加方便快捷的加入新Serial，并且你已经可以添加了，我已经配置好了
5. 开始进行plan吧

+++

进入D:\github\HUST_STI\HUST_STI_1\Templete\Template_STM32H743VIT6\模板工程文件夹配置，修改Serial_使用说明.md，原先的只是参考，你需要删除然后填入当前的相关信息

























